def in_asc_order(arr):
    if arr==sorted(arr):
        f=True
    else:
        f=False
    return f
#on a utilisé la fonction sorted qui nous permet de mettre un tableau en ordre croissant
#on compare notre arr initiale et le arr apres l'utilisation de la fonction sorted

        
       
